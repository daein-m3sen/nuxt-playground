{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@types/node": "^18",
    "nuxt": "^3.4.2"
  },
  "dependencies": {
    "@nuxtjs/proxy": "^2.1.0",
    "dotenv": "^16.0.3",
    "sass": "^1.62.1",
    "socket.io": "^4.6.1",
    "socket.io-client": "^4.6.1"
  }
}
